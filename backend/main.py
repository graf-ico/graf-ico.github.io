

def main():
    return
    # users = {}
    # for a in usersA:
    #     if a not in users:
    #         users[a] = {groupA: False, groupB: False}
    #     users[a][groupA] = True

    # for b in usersB:
    #     if b not in users:
    #         users[b] = {groupA: False, groupB: False}
    #     users[b][groupB] = True

    # shared = 0
    # for u in users:
    #     if users[u][groupA] and users[u][groupB]:
    #         shared += 1

    # print(str(shared) + " shared users across " +
    #       groupA + " and " + groupB + ".")
    # print(str(int(shared/len(usersA) * 100)) + "% of " +
    #       str(len(usersA)) + " " + groupA + " users are also in " + groupB)
    # print(str(int(shared/len(usersB) * 100)) + "% of " +
    #       str(len(usersB)) + " " + groupB + " users are also in " + groupA)


if __name__ == "__main__":
    main()
